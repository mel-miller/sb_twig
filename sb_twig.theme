<?php

/**
 * @file
 * Functions to support theming.
 */

 /**
 * Implements hook_preprocess().
 */
function sb_twig_preprocess(array &$variables, $hook) {
  //all-purpose variables
  $variables['base_path'] = base_path();
  $variables['sb'] = 'sb-twig-poc-composer';
}
