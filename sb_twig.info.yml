name: SB Twig POC
type: theme
description: "Drupal theme to demo sb-twig-poc."
package: PDS
core_version_requirement: "^8.8 || ^9"

libraries:
  - sb_twig/global-css

ckeditor_stylesheets:
  - sb-twig-poc-composer/css/main.css

base theme: stable

component-libraries:
  sb:
    paths:
      - sb-twig-poc-composer/twig
